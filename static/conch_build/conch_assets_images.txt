以下是本次编译conch产物中可能用到的图片节点，请检查使用到的图片，确保与基准包不一致的图片与产物一起打包下发：

id()==> StaticGet(GSYICons.DEFAULT_USER_ICON)
id()==> InstanceGet(InstanceAccessKind.Instance,uri.{Uri.path})
id()==> StaticGet(GSYICons.DEFAULT_IMAGE)
id()==> StringLiteral("static/images/welcome.png")
id()==> StringLiteral("static/images/logo.png")
